<div class="mx-auto md:w-2/3 w-full mt-10">
  <h1 class="font-bold text-4xl text-white">Edit project</h1>

  <%= render "form", project: @project %>


  <div class="flex items-center justify-center mt-5">
    <%= link_to 'Cancel', :back, class: "text-sm font-semibold text-white rounded-lg py-2 px-4 bg-redBtn" %>
  </div>
</div>
